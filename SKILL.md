---
name: getnote
description: 使用得到大脑保存文字、链接和图片，查看、搜索、修改或整理笔记、标签与知识库。用户说“记一下”“保存这个链接/图片”“最近有哪些笔记”“帮我找笔记”“查看原文/转写/附件”“修改/分享这条笔记”“整理到知识库/文件夹”“订阅抖音博主”或需要连接、诊断、升级得到大脑时使用。所有真实操作通过 getnote CLI 完成。
metadata:
  {
    "openclaw":
      {
        "emoji": "🧠",
        "requires": { "bins": ["getnote"] },
        "install":
          [
            {
              "id": "node",
              "kind": "node",
              "package": "@getnote/cli",
              "bins": ["getnote"],
              "label": "安装得到大脑执行组件",
            },
          ],
      },
  }
---

# 得到大脑

理解用户意图、安排安全的操作流程，并通过 `getnote` CLI 完成真实操作。不要自己拼接 OpenAPI 请求、猜参数、编造 ID 或伪造成功结果。

## 开始前

1. 正常任务直接执行，不要每次都运行诊断。
2. 安装本 Skill 或用户明确要求“安装/开始使用得到大脑”时，自动安装缺少的 `getnote` 执行组件并继续原任务，不再要求用户理解或手动安装依赖。只有平台弹出系统级安装确认时，才提示用户完成该次确认。
3. 尚未授权时运行登录流程，让用户在浏览器确认账号；不得索要 API Key、Cookie 或 Authorization。
4. 首次安装、刚完成升级或出现异常时运行诊断；只有需要核对兼容性时才读取 capabilities。
5. 不确定命令参数时运行对应命令的 `--help`。CLI 帮助和结构化输出是命令事实源，本 Skill 不代替参数文档。

## 识别意图

| 用户想做什么 | CLI 入口 |
|---|---|
| 保存文字、长内容、链接或图片 | `getnote save` |
| 查询一次异步保存 | `getnote task` |
| 查看最近笔记 | `getnote notes` |
| 查看笔记详情 | `getnote note` |
| 读取链接/文字原文 | `getnote note original` |
| 读取录音、会议或课堂转写 | `getnote note transcript` |
| 读取附件、时间线或快捷笔记 | `getnote note attachments` / `getnote note timeline` / `getnote note quick-note` |
| 读取会议总结中的派生待办 | `getnote note todos` |
| 修改、删除或分享笔记 | `getnote note update` / `getnote note delete` / `getnote note share` |
| 按主题或自然语言找笔记 | `getnote search` |
| 查看自有或订阅知识库 | `getnote kbs` / `getnote kbs-sub` |
| 查看或维护知识库 | `getnote kb` |
| 浏览和维护知识库文件夹 | `getnote kb directories` / `getnote kb directory-create` / `getnote kb directory-update` / `getnote kb directory-delete` |
| 把笔记加入指定知识库或文件夹 | `getnote kb add` |
| 查看博主内容或订阅抖音博主 | `getnote kb bloggers` / `getnote kb blogger-contents` / `getnote kb blogger-content` / `getnote kb blogger-follow` |
| 查看或订阅直播 | `getnote kb lives` / `getnote kb live` / `getnote kb live-follow` |
| 查看、添加或删除标签 | `getnote tag` |
| 查看配额 | `getnote quota` |
| 登录、检查连接或退出 | `getnote auth` / `getnote doctor` |
| 检查并升级得到大脑 | `getnote update` |

## 保存笔记

### 文字

1. 从用户原话中提取要保存的内容，不擅自扩写或改变含义。
2. 长内容通过文件或标准输入交给 CLI，避免命令行截断和转义损坏。
3. 用户没有指定时，不擅自添加知识库、目录、父笔记、标签或公开分享。
4. 只有 CLI 返回真实成功结果后，回复标题和 `note_url`。

### 链接

1. 用户给出网页 URL 并表达保存意图时走链接保存，不要把 URL 当普通文字。
2. 普通网页保存可能异步处理；持续等待 CLI 给出最终状态。
3. 处理中可以告诉用户正在抓取和生成，但不能提前说保存成功。
4. 完成后只返回 CLI 给出的标题和 `note_url`，不要自行拼链接。

### 图片

1. 用户上传本地图片并表达保存意图时走图片保存，不要只把图片文件名写进文字笔记。
2. 多张图片按用户这一轮的明确范围处理，不私自带上历史图片。
3. 等待上传、识别和笔记生成全部完成；中间状态不能算成功。
4. 上传或处理结果不确定时先查询原任务，不重复创建笔记。

### 安全重试

- 同一次创建的重试复用原幂等键。
- 已提交写操作但结果不确定时，先查询任务或最近结果；禁止盲目再写一次。
- CLI 明确标记可重试时才重试；否则保留错误原因和 `request_id`。

## 查看和查找笔记

1. “最近、列表、有哪些笔记”使用最近笔记，不要降级成语义搜索。
2. “找、搜、关于某主题”使用语义搜索。
3. 用户给出笔记 ID 或私有笔记链接时查看该笔记；所有 ID 按字符串原样传递。
4. 列表和搜索结果按需展示标题、字符串 ID 和真实 `note_url`；不要先展开所有正文。
5. 用户继续追问某一条时，再读取对应详情或一级内容视图。

### 原文和深层内容

- 链接或文字笔记的“原文”使用 original。
- 录音、会议、课堂的“原文/逐字稿”使用 transcript。
- 图片、音频和文件使用 attachments。
- 时间点、分段或会议过程使用 timeline。
- 用户现场记录的快捷内容使用 quick-note。
- 不确定笔记类型时先读详情再选择；不得拿 AI 摘要冒充原文。
- 会议待办使用 `getnote note todos`，并向用户保留其来源说明；只有 CLI 返回的派生结果才能作为待办展示，不得根据普通总结自由猜测。

## 修改、删除和分享

1. 修改前读取目标笔记及版本，确认用户指向的是哪一条。
2. 追加或前置内容要使用对应的增量语义，避免覆盖原正文。
3. 覆盖正文、替换全部标签、删除笔记和公开分享必须先向用户确认。
4. 用户确认后按 CLI 帮助使用显式放行参数；不得绕过 CLI 的冲突和版本保护。
5. 用户没有要求公开时只返回私有 `note_url`；不要为了方便访问自动创建分享链接。

## 知识库和文件夹

1. 先读取真实知识库列表，再根据名称和 `scope` 选择，不能假设只有默认知识库。
2. DEFAULT、BOOKSPACE、CUSTOMER、TEAMSPACE 都应保留；同名时让用户选择，不猜 ID。
3. 用户要求放入文件夹时，先读取目录树；已有目录直接使用，缺少目录时再询问是否创建。
4. 加入笔记时同时使用真实知识库 ID 和目录 ID，不用名称替代。
5. 团队知识库成员可读；只有拥有者或管理员可以维护目录或加入笔记。权限不足时明确说明，不伪装成功。
6. 当前不替用户新建团队知识库；产品归属和管理员授权未明确时不要尝试绕过。
7. 移出笔记和删除目录属于高风险操作，执行前确认；删除目录还需让 CLI 校验是否为空。

## 抖音博主和直播

1. 用户给出抖音博主主页并要求持续关注时，先确认目标知识库，再创建博主订阅。
2. 只想找某条博主内容时先查询内容，不创建订阅。
3. 写入订阅前检查目标知识库写权限；团队普通成员不能代替管理员订阅。
4. 查询博主内容或直播时先返回精简列表，用户选中后再读完整详情或原文。

## 标签

1. 先读取真实标签列表或笔记现有标签，不凭名称编造标签 ID。
2. 添加标签只增加用户指定项，不覆盖其他标签。
3. 删除标签使用真实标签 ID；替换全部标签前必须确认。

## 机器执行与结果判断

- 机器调用使用结构化输出，以进程退出码和业务结果为准；HTTP 成功不等于业务成功。
- 笔记、知识库、目录、任务、标签和游标 ID 全程按字符串传递。
- 只使用 CLI 返回的环境正确链接，不自行拼接生产或测试域名。
- 保存成功必须有最终成功状态；链接和图片还要确认异步任务完成。
- 安装完成不等于账号授权完成，授权完成也不等于测试保存成功。

## 回复用户

- 保存成功：标题 + 可点击 `note_url`，必要时简短说明保存到哪个知识库/目录。
- 列表或搜索：标题 + 字符串 ID + `note_url`，按用户要求控制数量。
- 修改或归档：说明实际改动对象和结果，不只说“已处理”。
- 失败：说明失败步骤、真实原因、是否可重试和下一步；保留 `request_id`。
- 群聊或共享会话中不主动展开私密全文，先确认请求者和展示范围。

## 更新得到大脑

当用户说“帮我更新得到大脑”时：

1. 检查当前 CLI 与 Skill 是否有可用更新。
2. 用户这句话已构成更新授权，自动完成能够自动处理的组件更新，不再要求用户选择或手动运行依赖命令。
3. 更新后运行诊断，并实际验证一次读取能力；用户允许时再保存测试笔记。
4. 若 Skill 必须由平台技能市场更新，引导用户完成唯一必要的点击，不把内部组件差异抛给用户。
5. 最后只告诉用户是否已更新、验证是否通过，以及仍需完成的动作。

## 安全边界

- 不索要、记录或展示 API Key、Authorization、Cookie 和完整凭证。
- 不编造 API、命令参数、ID、链接、处理状态或结果。
- 不在未确认时执行删除、覆盖、替换全部标签、公开分享或批量移出。
- 不因一次失败自动重复写入；优先核实已有任务和结果。
- 不把团队成员的读取权限误当成写入权限。
